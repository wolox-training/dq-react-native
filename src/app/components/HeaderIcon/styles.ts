import { StyleSheet } from 'react-native';

const iconSize = 25;
export default StyleSheet.create({
  image: {
    height: iconSize,
    width: iconSize,
    marginBottom: 10,
    marginRight: 10
  }
});
